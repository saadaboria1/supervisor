# دليل تشغيل وربط قاعدة بيانات سوبابيز (Supabase Guide)
## منظومة الإشراف التربوي الذكي - مدارس المدينة الأكاديمية

يقدم هذا الدليل خطوات مبسطة وعملية لربط المنظومة بقاعدة بيانات سحابية حقيقية وسريعة ومجانية عبر **Supabase (PostgreSQL)**.

---

### 1. إنشاء حساب ومشروع مجاني على Supabase
1. توجه إلى موقع [supabase.com](https://supabase.com) وسجل الدخول (أو أنشئ حساباً جديداً مجاناً).
2. اضغط على **"New Project"**.
3. املأ بيانات المشروع:
   * **Name**: `Academic-City-Supervision` (أو أي اسم تفضله).
   * **Database Password**: اختر كلمة مرور قوية واحتفظ بها.
   * **Region**: اختر أقرب منطقة جغرافية (مثل: `Frankfurt` أو `Bahrain / ME`).
4. اضغط على **"Create new project"** وانتظر دقيقة حتى يكتمل تجهيز الخادم السحابي.

---

### 2. تهيئة الجداول وتشغيل سكريبت الـ SQL
1. من القائمة الجانبية اليسرى في لوحة Supabase، اضغط على **"SQL Editor"** (أيقونة `>_`).
2. اضغط على **"New query"**.
3. افتح ملف **[supabase_schema.sql](file:///f:/mvp/supabase_schema.sql)** الموجود في مجلد المشروع، وانسخ كافة محتوياته والصقها في محرر الاستعلامات.
4. اضغط على زر **"Run"** (أو اضغط `Ctrl + Enter`).
5. ستظهر رسالة `Success: No rows returned`، مما يعني:
   * ✅ تم إنشاء كافة الجداول العلائقية العشرة (`profiles`, `teachers`, `weekly_plans`, `classroom_observations`, `improvement_actions`, إلخ).
   * ✅ تم تفعيل سياسات الأمان (Row Level Security - RLS).
   * ✅ تم إدراج البيانات الأولية لكادر مدارس المدينة الأكاديمية والخطط والزيارات.

---

### 3. نسخ مفاتيح الربط (API Credentials)
1. من القائمة الجانبية في لوحة Supabase، اضغط على **"Project Settings"** (أيقونة الترس ⚙️) أسفل القائمة.
2. اختر قسم **"API"**.
3. ستجد عنصرين أساسيين:
   * **Project URL**: يبدأ بـ `https://xxxx.supabase.co` $\rightarrow$ اضغط **Copy**.
   * **Project API keys**: انسخ المفتاح العام المسمى **`anon` / `public`**.

---

### 4. تفعيل الاتصال من واجهة المنظومة
1. افتح صفحة النظام الرئيسية `index.html` أو صفحة `pages/settings.html`.
2. انتقل إلى تبويب **"الإعدادات وتكامل الأنظمة ⚙️"**.
3. في بطاقة **"الربط السحابي وقاعدة البيانات (Supabase PostgreSQL)"**:
   * الصق **Project URL** في خانة عنوان المشروع.
   * الصق **Anon Key** في خانة مفتاح الوصول العام.
4. اضغط على زر **"فحص واختبار الاتصال"** للتأكد من استجابة الخادم وسرعة الاستجابة (Latency).
5. اضغط على زر **"حفظ وتفعيل المفاتيح"**.
6. (اختياري) يمكنك الضغط على **"رفع ومزامنة البيانات الحالية (Seed Sync)"** لرفع أحدث التعديلات المحلية مباشرة إلى السحابة.

---

### 5. كيف يعمل النظام الهجين (Hybrid Cloud / Local)؟
* **في حالة عدم إدخال مفاتيح:** تعمل المنظومة تلقائياً وبأقصى كفاءة على وضع **التخزين المحلي الآمن (LocalStorage)** دون أي تعطل.
* **عند تفعيل Supabase:** يتحول النظام تلقائياً لمزامنة كافة الخطط والتقييمات والإجراءات سحابياً ولحظياً، مع استمرار العمل حتى في حال انقطاع مؤقت للإنترنت.
